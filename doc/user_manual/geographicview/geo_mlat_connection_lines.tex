\subsection{MLAT Remote Unit Connection Lines}
\label{sec:geo_mlat_connection_lines}

For MLAT target reports, the Geographic View can draw connection lines from each report to its contributing Remote Units. This follows the same pattern as other optional informative geometries such as Ground Speed Vectors and Position Accuracy Ellipses. \\\\

The lines are only available where the source data contains contributing receiver information and where the parent MLAT Data Source has Remote Units defined (see \nameref{sec:ui_configure_ds_mlat_rus}). \\

The connection lines can be toggled in three places: \\

\begin{itemize}
\item Globally via the 'Other' tab: the 'Show MLAT Contributing RUs' checkbox enables or disables MLAT connection lines for the entire view
\item Per target report via the right-click context menu on a report: 'Show MLAT Contributing RUs' and 'Clear MLAT Contributing RUs'
\item Per layer or geometry item via the right-click context menu on a layer or geometry node (Parent UTN, Parent DBContent CAT020, Parent Root Geometry): same 'Show MLAT Contributing RUs' / 'Clear MLAT Contributing RUs' actions
\end{itemize}
\ \\

The context-menu actions only appear on items whose underlying data contains contributing receivers. \\

\begin{figure}[H]
  \centering
    \fbox{\parbox{0.8\textwidth}{\vspace{1.5cm}\centering \textcolor{red}{\textbf{[FIGURE TODO: 'Other' tab with the 'Show MLAT Contributing RUs' checkbox enabled]}}\vspace{1.5cm}}}
  \caption{MLAT Connection Lines: Other tab global toggle}
\end{figure}

\begin{figure}[H]
  \centering
    \fbox{\parbox{0.8\textwidth}{\vspace{1.5cm}\centering \textcolor{red}{\textbf{[FIGURE TODO: right-click context menu on a target report showing the Show / Clear MLAT Contributing RUs actions]}}\vspace{1.5cm}}}
  \caption{MLAT Connection Lines: per target-report toggle}
\end{figure}

\begin{figure}[H]
  \centering
    \fbox{\parbox{0.8\textwidth}{\vspace{1.5cm}\centering \textcolor{red}{\textbf{[FIGURE TODO: right-click context menu on a UTN / CAT020 / Root Geometry layer node showing the Show / Clear actions]}}\vspace{1.5cm}}}
  \caption{MLAT Connection Lines: per layer toggle}
\end{figure}

\begin{figure}[H]
  \centering
    \fbox{\parbox{0.8\textwidth}{\vspace{1.5cm}\centering \textcolor{red}{\textbf{[FIGURE TODO: map view with per-UTN MLAT connection lines drawn from target reports to their contributing RUs]}}\vspace{1.5cm}}}
  \caption{MLAT Connection Lines: result on the map}
\end{figure}
