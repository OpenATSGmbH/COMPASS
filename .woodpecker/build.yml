steps:
  - name: step1
    image: alpine
    environment:
      GITHUB_TOKEN:
        from_secret: github_token
    commands:
      - echo "token length ${#GITHUB_TOKEN}"

  - name: step2
    image: alpine
    commands:
      - echo "step 2"
